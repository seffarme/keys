<h1>Liste des biens</h1>

<ul>
    <% @biens.each do |bien| %>
        <li>
            <%= link_to bien.nom, bien_path(bien) %>
        </li>
    <% end %>
</ul>

<div class="">
  <div class="d-flex justify-content-around align-items-center">
    <div>
      <%= link_to "Ajoutez un nouveau bien 🚀", new_bien_path%>
    </div>
  </div>
</div>
